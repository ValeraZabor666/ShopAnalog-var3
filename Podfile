# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ShopAnalog(var 3)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ShopAnalog(var 3)

pod 'SnapKit'

  target 'ShopAnalog(var 3)Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ShopAnalog(var 3)UITests' do
    # Pods for testing
  end

end
